package lc.common;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public abstract class LcController {
	protected Log log = LogFactory.getLog(getClass().getName());
}
